import React from "react";
import { View, Text, SafeAreaView } from "react-native";

const MenuScreen112 = () => {
  return (
    <SafeAreaView style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Menu Screen 1 1 2!</Text>
    </SafeAreaView>
  );
};

export default MenuScreen112;